trigger:
  batch: true
  branches:
    include:
      - dev/ssdt-*

stages:
- stage: Build
  displayName: Build SQL Database Project
  variables:
  - template: 'variables/ssdt-generic.yml'
  jobs:
  - template: templates/build.yml

- stage: DeployDev
  displayName: Deploy to DEV 
  variables:
  - template: 'variables/ssdt-generic.yml'
  jobs:
  - template: templates/deploy.yml
